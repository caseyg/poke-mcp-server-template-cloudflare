# Cloudflare Workers configuration for MCP Server
name = "poke-mcp-server"
main = "src/index.ts"
compatibility_date = "2024-12-01"

workers_dev = true

[build]
command = "bun build src/index.ts --outfile=dist/index.js --format=esm --target=browser"

[vars]
ENVIRONMENT = "production"
