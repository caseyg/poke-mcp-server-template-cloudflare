{
  "name": "casey-mcp-server",
  "version": "1.0.0",
  "description": "MCP Server for Casey Gollan's public information",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "bun build src/index.ts --outfile=dist/index.js --format=esm --target=browser",
    "start": "wrangler dev",
    "test": "bun test",
    "type-check": "bun x tsc --noEmit"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "cloudflare",
    "workers"
  ],
  "author": "Casey Gollan",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
    "@cloudflare/workers-types": "^4.20241127.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.86.0"
  }
}
